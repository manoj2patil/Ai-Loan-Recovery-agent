# Prompt for Emergent — Real-Time Turn-by-Turn Voice Agent

Paste the block below into Emergent. It tells Emergent to refactor your existing voice pipeline
from the laggy `<Record>` approach to **Twilio Media Streams** (real-time streaming), so the agent
converses turn-by-turn with ~1–2s latency and barge-in, like Sarvam's Samvaad finance demo.

> IMPORTANT CAVEAT (read first): the real-time audio WebSocket must run as a **persistent
> process** — serverless/sandbox functions cannot hold a live socket (your own dev log noted the
> process gets killed). So the prompt tells Emergent to build the media server as a **standalone
> deployable service** that you host on an always-on machine (your on-prem App server or a small
> VM/container), and point Twilio's `<Stream>` at its public `wss://` URL. Emergent builds the
> code; you run that one service outside the serverless app.

---

## PASTE THIS INTO EMERGENT

```
GOAL
Upgrade my existing loan-recovery voice agent to REAL-TIME, TURN-BY-TURN conversation with
~1–2 second latency and barge-in, so it talks naturally like Sarvam's Samvaad finance/collections
agent (fluid, empathetic, multilingual, interruptible). Fix the current lag.

STEP 0 — BACKUP FIRST
Create a new branch "voice-media-streams" and commit the current working state before ANY change.
Do not modify the main branch. I must be able to roll back.

ROOT CAUSE OF CURRENT LAG
The voice flow uses Twilio <Record> (stop → upload → ASR → LLM → TTS → play), which adds ~4–5s of
silence per turn. Replace it with Twilio MEDIA STREAMS (real-time bidirectional audio over
WebSocket) and stream every stage.

ARCHITECTURE TO BUILD
1) TwiML: replace the <Record> loop with:
   <Connect><Stream url="wss://<MEDIA_SERVER_HOST>/media"><Parameter name="phone" value="..."/>
   <Parameter name="lang" value="..."/></Stream></Connect>
   - Escape all URLs (& -> &amp;). Use ONLY streamed Sarvam audio; NEVER <Say>. NEVER <Gather>.

2) MEDIA WEBSOCKET SERVER — build as a STANDALONE Node (or Bun) service (its own process/port,
   Dockerized), because serverless functions cannot hold a WebSocket. It:
   - Accepts Twilio's 8kHz μ-law media stream.
   - Decodes μ-law → 16kHz PCM and streams it to Sarvam ASR (Saaras, streaming, language="unknown").
   - On (partial/final) transcript, calls Sarvam LLM (sarvam-30b) with STREAMING enabled.
   - Streams LLM output sentence-by-sentence into Sarvam TTS (Bulbul v3, 8kHz μ-law) and sends
     audio frames back to Twilio as they are generated.

3) STREAM EVERY STAGE (this is the fix): streaming ASR (partials), streaming LLM (start TTS on the
   first clause — do NOT wait for the full reply), streaming TTS (send chunks as produced). Never
   do sequential full-ASR → full-LLM → full-TTS.

4) VAD + BARGE-IN: use Silero VAD for fast endpointing (~300ms). If the borrower starts speaking
   while the agent is talking, IMMEDIATELY stop the agent's audio (send Twilio "clear") and listen.
   (Samvaad emits user_interrupt for this.)

5) AUTO LANGUAGE SWITCH: Saaras with language="unknown" detects language each turn. When it changes
   for ~2 sustained turns, switch the TTS language (keep the same voice). Handle Hinglish/code-mix
   naturally — do NOT hard-switch on a single mixed word.

6) DYNAMIC PROMPT (not scripted): use a SYSTEM PROMPT that scopes the agent to LOAN RECOVERY ONLY +
   persona "Asha" + RBI tone (empathetic, non-coercive, no threats) + "NEVER invent figures" +
   off-topic redirect + never ask for OTP/PIN. Inject the borrower's live loan facts (name, EMI,
   due date, days overdue, pending amount) as variables each call. The LLM must converse DYNAMICALLY
   within this — no canned scripts. [I will paste the full Asha system prompt + variable list.]

7) STAY ON LOAN ONLY: if the borrower goes off-topic, give one short line then steer back to the
   loan. Verify identity (last 4 of Aadhaar) before revealing any loan detail. All figures come from
   my database, never generated by the model.

MODELS
ASR: Sarvam Saaras (streaming). LLM: Sarvam sarvam-30b (streaming). TTS: Sarvam Bulbul v3, voice
"anushka", pace 0.9, and DO NOT set pitch or loudness on Bulbul v3 (it returns 400).

KNOWN GOTCHAS — DO NOT REPEAT
- Only streamed Sarvam audio; NEVER <Say> (causes a second robotic voice).
- Escape & -> &amp; in every TwiML URL.
- NEVER use Twilio <Gather> ASR for Indic — it cannot transcribe Marathi and hallucinates Hindi.
- Pre-generate and cache the greeting TTS so the opening is instant.
- Make DB writes async/fire-and-forget so they never block the audio loop.
- Convert audio correctly: Twilio is 8kHz μ-law; ASR wants 16kHz PCM; TTS back to 8kHz μ-law.

ACCEPTANCE CRITERIA (test with a real Twilio call and iterate until all pass)
- Per-turn response is ~1–2s, not 4–5s.
- The borrower can interrupt mid-sentence and the agent stops immediately.
- Mid-call language switching works: Hindi ⇄ Marathi ⇄ English.
- The agent stays strictly on loan topics; all amounts/dates match the database.
- The back-and-forth feels natural and continuous, like the Samvaad finance demo.

DELIVERABLES
- The standalone media-stream WebSocket service (with Dockerfile + run instructions).
- Updated TwiML routes using <Connect><Stream>.
- Config for the Sarvam streaming ASR/LLM/TTS clients.
- A short README: how to deploy the media server on an always-on host and set MEDIA_SERVER_HOST.
Then place a test call, measure per-turn latency, and keep optimizing until ~1–2s with barge-in.
```

---

## After you paste it, also give Emergent:
1. Your **Asha system prompt + variable list** (the `samvaad-agent-config-skvcb.md` content) for step 6.
2. Your **DB field names** so it maps loan facts into the prompt variables.
3. The **public host** where the media WebSocket server will run (its `wss://` URL for `<Stream>`).

## If Emergent can't host a persistent WebSocket
That's expected. Have Emergent build the media-stream server as a standalone service and run it on
your **on-prem App server** (systemd) or a small always-on container/VM. The Emergent app only
needs to (a) place the Twilio call and (b) return TwiML pointing `<Stream>` at that server's URL.
